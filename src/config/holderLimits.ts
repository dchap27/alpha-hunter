/** Default number of holder accounts requested per page. */
export const DEFAULT_HOLDER_LIMIT = 100;

/** Maximum number of holder accounts allowed in one request. */
export const MAX_HOLDER_LIMIT = 1_000;
